# Compose configuration. Copy to your own values for production.
REACT_APP_API_URL=http://localhost:4023
# Frontend
WEB_PORT=3023
# Also picked up by `npm start` (CRA dev server) when running the frontend
# outside Docker.
PORT=3023
